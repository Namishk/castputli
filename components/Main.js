import react, {useState} from "react";
import { View, Text, Button, TextInput, StyleSheet, TouchableOpacity } from "react-native";

export default function Main({navigation}){
    return(<>
    <Text>main contents of application</Text>
    </>);
}